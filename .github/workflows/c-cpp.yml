name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: export GPP='g++ --std=c++14';make all
    - name: sample test
      run: ./sample.out -d
    - name: basic_tests
      run: ./basic_tests.out -d
    - name: benchmark
      run: ./benchmark.out -d
